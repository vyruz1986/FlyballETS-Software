import { CrossingTime } from './crossing-time.pipe';

describe('CrossingTime', () => {
  it('create an instance', () => {
    const pipe = new CrossingTime();
    expect(pipe).toBeTruthy();
  });
});
