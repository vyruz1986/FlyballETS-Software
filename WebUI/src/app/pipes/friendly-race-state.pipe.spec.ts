import { FriendlyRaceStatePipe } from './friendly-race-state.pipe';

describe('FriendlyRaceStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FriendlyRaceStatePipe();
    expect(pipe).toBeTruthy();
  });
});
