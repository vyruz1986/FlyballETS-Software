export enum RaceStateEnum {
    STATE_RESET = 0,
    STATE_STARTING = 1,
    STATE_RUNNING = 2,
    STATE_STOPPED = 3
}

export enum RaceCommandEnum {
    CMD_START = 0,
    CMD_STOP = 1,
    CMD_RESET = 2,
    CMD_UPDATE = 3
}