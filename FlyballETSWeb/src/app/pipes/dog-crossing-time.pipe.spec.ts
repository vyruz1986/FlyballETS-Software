import { DogCrossingTimePipe } from './dog-crossing-time.pipe';

describe('DogCrossingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DogCrossingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
